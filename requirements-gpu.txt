# GPU-accelerated version requirements
# Includes all base requirements plus GPU support

-r requirements.txt

# Additional docling-parse for GPU
docling-parse>=2.0.0

# GPU acceleration (CUDA support)
torch>=2.0.0
torchvision>=0.15.0

# Optional GPU-accelerated libraries
cupy-cuda12x>=12.0.0  # For CUDA 12.x, adjust version as needed